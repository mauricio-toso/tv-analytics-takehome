# Postgres connection configuration
# Copy this file to .env and replace with actual values (though defaults work for local dev)

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dash247
POSTGRES_USER=dash247
POSTGRES_PASSWORD=dash247_dev

# Connection string format (if needed by your client):
# postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
